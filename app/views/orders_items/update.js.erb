// Refresh the checkout information
var checkoutInfo = "Subtotal (<%= "#{current_user.cart_size} #{current_user.cart_size == 1 ? "item" : "items"}" %>): <%= humanized_money_with_symbol(current_user.cart_total) %>"
document.querySelector(".order-checkout p").innerHTML = checkoutInfo;

// Update the number of orders in the cart
document.querySelector('#cart-size a').innerHTML = "Cart (<%= current_user.cart_size %>)";
