// Close the Modal
document.querySelector('#ModalDeleteCategory<%= @category.id %> button').click();

// Remove the Modal's background shadow
document.querySelector('body').classList.remove('modal-open');
document.querySelector('.modal-backdrop').remove();

// Remove the Category row from the table
document.getElementById('category_row_<%= @category.id %>').remove();
