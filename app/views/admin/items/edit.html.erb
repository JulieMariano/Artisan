<%= simple_form_for [:admin, @category, @item] do |f| %>
  <%= f.input :name %>
  <%= f.input :price %>
  <%= f.input :description %>
  <%= f.input :picture, as: :file %>
  <%= f.association :category, collection: Category.all %>
  <%= f.submit "Add", class: "btn btn-primary" %>
<% end %>
