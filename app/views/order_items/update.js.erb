// Refresh the Orders#index page relevant fields
var shoppingCart = "<%= j render partial: "orders/shopping-cart" %>";
var navbarCart = "Cart <%= "(#{current_user.cart_size})" unless current_user.cart_size.zero? %>";
refreshOrdersIndexPage(shoppingCart, navbarCart);
